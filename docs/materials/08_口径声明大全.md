# 素材包 08 — 口径声明大全（论文手防"口径双标"护身符）

> 每个数字的口径/边界/假设集中声明——论文引用任何数字时对照本表。
> 原则：主口径唯一；伴随口径明确标注；修正历史可追溯（sum_12）。

## 一、全局口径（不可变）

| # | 口径 | 内容 | 来源 |
|---|---|---|---|
| G1 | 任务-电力映射 | AI_IT_Load = Σ GPU_Demand×Overlap×GPU_Power(TaskType)，功率 0.08/0.10/0.16 MW/GPU | 附件 1 唯一口径 |
| G2 | 负荷构成 | IT_Load = NonAI + AI；Total_Load = IT×PUE | 附件 1 |
| G3 | 功率平衡 | G + W + Pd = D + Pc + S + Q（Q=弃电松弛） | 附件 1 |
| G4 | SOC 递推 | SOC(t)=SOC(t−1)+ηc·Pc(t)−Pd(t)/ηd（分区效率：A/B/C 0.93/0.92；D/E/F 0.94/0.93） | R3 + 官方 |
| G5 | 时域 | 0-2399 决策；2400-2405 结清（禁充）；2406 终态（不安排任务） | 说明 6 |
| G6 | 白名单 | RT≤20 / BI≤80 / AT≤150 ms（预计算可达集合） | 数据实证 |
| G7 | 消纳模板 | U = c_r(h)·D（命中率 100%）；cap_h = min(W, c_r(h)·D)；Closure 段 cap_h=min(W,D) | R1 + sum_10 |
| G8 | 任务刚性 | 实时推理到达即开工；任务不可抢占/拆分/中途迁移 | D8 + 说明 |
| G9 | 预测切分 | 0-2351 训练 / 2352-2375 调参 / 2376-2399 冻结测试；调度输入=实际到达任务 | 说明 2/5 |
| G10 | RegionE SOC | 以储能递推公式重新计算（官方推荐措辞；起点误差 ~1 MWh 经递推传播为全程平移） | 官方 + step0.5 |

## 二、Q3 口径

| # | 口径 | 内容 |
|---|---|---|
| Q3-1 | D 固定 | D = (Baseline_AI + NonAI)×PUE（任务层冻结，说明 4） |
| Q3-2 | 主模型 | M3_final = 时段状态机 + 结算段禁充 + 终态严格 SOC(2406)=Init + 生成器量级斜坡（A 66/45 … F 145/163） |
| Q3-3 | 价值区间 | [M3_final（保守）, M0x（物理上界）]；M0（无互斥）含传送带假象**不得引用** |
| Q3-4 | **双锚对照** | 储能价值 = M3_final − **无储能 LP**（solve_m3 零储能同框架）；模板对照含 LP 自由度红利（D 1.43 亿）只作参考 |
| Q3-5 | 波动/峰值指标 | 一律主时段 0-2399（X13：Closure 边界污染 385→195）；max 配 p95/p90 分布 |
| Q3-6 | 斜坡 | 生成器量级（数据实测标定，非统一假设值）；斜坡 60 昂贵/120-180 免费区 |
| Q3-7 | 改进率 | 双锚：vs 生成器（单锚）与 vs 无储能（双锚）分开声明；E/F 负成本基数用绝对值 |
| Q3-8 | 波动场景 | 64 原始场景（AR(1) φ=0.8，clip [0.1,2.0]）条件期望；K-means 中心截断低估 25.6% 已弃用 |
| Q3-9 | CVaR | 可行域内计算 + infeasible 占比双报告（E/F 尾部瓶颈） |
| Q3-10 | 终端对称性 | 生成器 SOC(2406)≠Init（D/E/F 违规）——优化侧终态严格是单侧更严口径 |

## 三、Q4 口径

| # | 口径 | 内容 |
|---|---|---|
| Q4-1 | D 随调度变化 | D(r,t) = (NonAI + 调度后 AI)×PUE（与 Q3 本质区别） |
| Q4-2 | 六指标 | Cost/Carbon 全 2407h；Lat/QOS 任务级；NU/P_peak 主时段；QOS α=0.5 固定（评估器参数不可优化） |
| Q4-3 | 上层策略 | 6 旋钮 = Q2 五旋钮 + α 固定 0.5；viol>0 → 1e12 绝对不可行 |
| Q4-4 | 正式预算 | 40×30×3（收敛：MC 超体积末 5 代 <5σ；种子方差 <0.1%） |
| Q4-5 | 影子价格 | 数值差分为准（储能 LP 退化，对偶向量不可投产）；仅对给定调度成立 |
| Q4-6 | 碳限额 | 等比例区域限额 τ≥1% 结构性不可达（6 旋钮 0.239%/构造 1.4%） |
| Q4-7 | 波动峰值 | 期望=max_r Σwᵢ峰ᵢ（条件）/最坏=max_r maxᵢ峰ᵢ；64 原始场景 |
| Q4-8 | 全网峰值 | 伴随口径（观测/敏感性）；题面区域级为主；削峰 5% infeasible |
| Q4-9 | c_h 外推 | R1 模板在 Q4 的假设（与负荷组成无关）；±10% 敏感性非对称（E/F 悬崖） |
| Q4-10 | NU 差异 | Q3（基线 D）33.9% vs Q4（调度 D）35.48%——口径不同不可直接比 |

## 四、场景与假设声明

| # | 场景/假设 | 声明 |
|---|---|---|
| S1 | 波动场景 | 题面未给场景数据——自造声明（AR(1) 扰动模板），档位 σ∈{10,20,30%} 继承 Q3 |
| S2 | 压力矩阵 | 碳限额/峰谷比/波动三档的档位依据（Sobol 归因 + 预扫 + Q3 继承） |
| S3 | 峰谷比重构 | p' = μ_seg + k(p−μ_seg)，保均值（均值漂移=0），失真检查 ≤10 |
| S4 | 循环退化 | 0.02%/等效循环为假设参数——仅量级参考 |
| S5 | 扰动自造 | MPC/波动实验的扰动均为自造（数据 W 确定性）——方法演示声明 |
| S6 | 无潮流 | 说明 7——区域独立求解成立；全网峰值仅观测 |

## 五、结论分级速查（引用时标注）

| 分级 | 定义 | 本项目代表 |
|---|---|---|
| 【实证】 | 数字直接支持+可复现+三关全过 | T1/T2 定理、双锚、结构锁定、碳上限、退化、波动修正 |
| 【推断】 | 有证据的机理解释 | M0x 浅循环红利来源、斜坡=SOC 目标猜测 |
| 【推测】 | 未验证假设（显式标注） | 生成器斜坡的物理来源 |

**修正历史速查**：储能价值（#52）/波动成本（S4）/MPC 归因（#54-55）/
α（#45）/双锚（#52）——引用数字时对照 sum_12 修正声明。
