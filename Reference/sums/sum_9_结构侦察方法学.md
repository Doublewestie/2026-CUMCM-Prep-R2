# Reference/sums/sum_9 — 结构侦察方法学（agent 深度认知）

> 定位：S9 结构完备性全谱侦察沉淀的系统性教训。人类速查见 docs/sums/sum_9；
> 本篇面向 agent 认知重建：每条教训 = 案例 → 根因 → 迁移检查项。
> 与 math-methods 案例库同构，可直接并入。

## 教训清单（10 条）

| # | 教训 | 案例 | 迁移检查项 |
|---|---|---|---|
| 1 | **先排除自身实现错误，再声称结构发现** | 滚动窗口 hod 4-6 预测成 Flat 水平 → 误报"PricePeriod 标签时域漂移"；真因 = rolling hod 键错位 bug（len(seg)%24 假设窗口从 hod 0 起，#39） | 单点对比异常 → 先自查实现（索引/键/切片/编码），再谈数据规律；数据核查（全部段映射一致）优先于推断 |
| 2 | **perf/重构改动必须以全量重跑验收** | deep 预测 GPU 化后单序列测试通过，但全量重跑暴露 cat 维度 bug（#41，energy 126→108 假象）——bug 只在"预测长度超 actual"路径触发 | 任何行为等价性声明（"数字不变"）以全量产物 diff 为准；单序列/单路径测试不足为凭 |
| 3 | **RNG 全局状态是顺序依赖的** | deep 权重初始化/TabPFN 内部 RNG 无 seed → deep_lstm 通过数 9↔10 边界翻转（#42） | 每个随机组件独立 seed（torch.Generator/random_state）；每折 fit 前重置全局 RNG；可复现性以"两次运行 diff=0"验证 |
| 4 | **判定噪声门用相对量级，不用绝对量级** | c5 反证关 1e-4 舍入噪声误伤 rel 1e-9 恒等（#37） | 数据表 3 位小数舍入 = 绝对量级 1e-4；判定"恒等"用相对残差 <1e-6，相关性判据在残差低于噪声门时无意义 |
| 5 | **均值线性算子可与恒等式交换** | S2（IT 模板−AI 均值模板）≡ S0（NonAI 直接模板）逐时段恒等（max|差|=5e-8）——分层在纯预测口径无增益 | 结构等价先证代数恒等（I1 精确 + 算子线性），再解释增益；增益必须归因到"额外信息"而非"方法魔法" |
| 6 | **生成口径差异先于模型问题** | Baseline_AI 与 ceil 整点展开逐时差 15%（总量一致 1e-12）→ 破案 = 分数 Overlap（I11） | 重建/评估与参考列不一致 → 先试分数重叠/调度假设等生成口径变体，再怀疑数据错误 |
| 7 | **表内恒等式与生成口径恒等式的证明要求不同** | I1（表内）残差 1e-10 即实证，构造性重建仅附加证据；I10/I11（生成口径）须构造性验证 | 7 关协议区分 c4_required：表内恒等式不因重建失败降级（重建失败=新发现入口） |
| 8 | **训练段相关结构不可外推冻结段** | E 区 AI~购电相关 train +0.07 → frozen −0.47（翻转 0.5） | 用相关做特征/结论前，分段验证相关稳定性（段间差异矩阵）；段间翻转本身就是发现 |
| 9 | **短窗评估只承担不崩溃验证** | 冻结段 24h 排名一致率 0.119-0.167（运行间波动） | 短窗不裁决"谁第一"；集合级指标（Jaccard 0.797）承担类选稳定性裁决 |
| 10 | **负结果必须记录并界定适用域** | 滚动重估对价格无增益（1.27 vs 2.89，非单调回升）→ 适用域=单调漂移序列 | 方法失效 ≠ 方法无用；记录触发条件（序列单调性），防止重复探索 |

## 结构侦察工具箱（S9 沉淀）

- **恒等式证明**：step0.6_identity_proof.py（7 关协议，tests/test_identity.py 守卫）
- **全谱扫描**：step0.6_spectrum.py（漂移矩阵/相关段间差/周期谱/CUSUM 变点）
- **临界点**：step0.6_thresholds.py（饱和/边界触碰/分箱断点/互信息）
- **机制分解**：step0.6_mechanism.py（三因子分解/构造性重建）
- **分层实验**：step0.7_nonai_layered.py / step0.7_rolling_energy.py

## 适用边界

- 所有结论限定"本数据（合成生成器）"，不推广为真实世界规律
- 恒等式集为数据表内代数事实；生成口径（分数 Overlap/模板族）为生成器结构推断
- 冻结段仅作验证（三段协议）；探索发现的段间差异用于描述性陈述，不用于调参
